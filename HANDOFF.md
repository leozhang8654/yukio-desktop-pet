# 工作交接

## 用户目标与已确认选择

白发、蓝眼、深蓝与白色管家／女仆制服的动漫角色，名字“雪绪”。用户要一个独立桌面播放器，在**一只宠物**上显示不同活动动作。

| 活动 ID | 含义 | 用户选择 | 最终图条 |
| --- | --- | --- | --- |
| `thinking` | 思考／规划 | A：托腮思考 | `thinking.webp` |
| `read_file` | 读取文件 | B：桌前读书 | `read_file.webp` |
| `view_image` | 查看图像 | B：放大镜检查 | `view_image.webp` |
| `write_file` | 修改／写入文件 | B：纸上书写 | `write_file.webp` |
| `verify` | 测试／验证 | B：对照检查 | `verify.webp` |
| `read_web` | 阅读网页 | B：平板浏览 | `read_web.webp` |
| `respond` | 整理／输出回答 | B：递交报告 | `respond.webp` |
| `default_work` | 其他或未识别的工作 | 稳定电脑桌 | `computer-desk.png` |

这些文件都在 `assets/activities/`。空闲、被拎起来、左右跑动等基础动作在 `assets/base/`，不要把“空闲”和“未识别的工作”混为一类。

## 必须延续的用户反馈

1. 用户反感动画低帧数却大幅变化、动作瞬切、人物比例漂移和桌椅呼吸抽搐。
2. 左右跑动的图条要保留原版，不要改画成侧目或静止站立。**2026-09-18 起拖动不再用它**：用户要求改成被一只无形的大手拎着（见下面当天的记录）。
   图条仍在 `assets/base/running-left|right.webp`，播放器不再加载；要回退就把 `AnimationCatalog.usedBase` 换回来。
3. 旧的跳跃、开心／欢呼、蹲缩变形已撤掉。基础图条中的 `calm-blink` 是替代动作，并不是跳跃。
4. 七种动作属于同一只宠物，**不能再次安装成七个独立宠物条目**。曾错误这样做过，用户明确反对，七个重复条目已撤回。
5. 平板旧图把操作手指放在了背面，用户明确指出空间关系错误。包里 `read_web.webp` 已是修正版，不能用旧图回退。
6. 递交报告应做一次并停住，不要不停来回递纸。桌椅固定，身体不做夸张的呼吸缩放。

## 2026-09-13 进展：Claude 兼容版播放器

用户要求改为 **Claude 兼容版本**：播放器跟随 Claude Code 的活动（不是 Codex）。工程在 `YukioPlayer/`，说明见 `YukioPlayer/README.md`。

- 已实现：单实例透明置顶窗口、拖动时被无形大手拎着（单摆晃动）、菜单栏（状态／模拟演示／暂停跟随／大小／退出）、事件协议、活动路由、防抖 400 ms、最短保持 1.5 s、同类合并 3.5 s、报告停留 8 s、多会话焦点隔离、失联回退、Claude 工具分类器、只读转录跟随、可选 hooks 适配器、模拟演示、`.app` 打包脚本。
- 真实来源：只读 `~/.claude/projects/*/*.jsonl`（本地会话记录，非公开 API）。已在本机对当前会话实测：工具开始写入后约 0.13–0.2 秒可见，显示切换再加约 0.4 秒防抖。
- Claude Code 官方 hooks 适配已写好但**未启用**：需要修改 `~/.claude/settings.json`，等用户同意。
- 已验证：36 个自动测试通过；资源裁切检查通过；32 MB 真实转录回放；本机实时跟随；只运行一个实例。
- 未验证（无屏幕录制权限）：窗口实际外观、透明穿透点击、拖动手感、多显示器、Retina 清晰度——需用户本机目测。
- Codex 版适配未实现；事件协议可再接一个 Codex 适配器（本机 Codex 0.153.4 有会话 JSONL 和 hooks，可作为来源）。
- 2026-09-14：新增“失败”状态，直接使用基础图条 `failed.webp`（垂眼沮丧），未重画。工具报错或 API 报错时显示；用户拒绝授权、中断不算失败。原生 140 ms 循环会每 1.2 秒低落又恢复，播放器改为垂眼一次后停住。
- 2026-09-14：新增头顶小气泡（最宽 180 点、两行）：上行是大任务（Claude 会话标题，没有时用请求第一行）；下行是任务清单里进行中的一项加进度，没有清单时是当前活动（“编辑 main.swift”“$ swift test”）。文字跟随已防抖的动作，每段至少停留 1.2 秒；没有任务时淡出，菜单可关。
- 2026-09-14：动作改为“单张底图 + 局部平滑变形”（`YukioPlayer/tools/motion/`）。原 4 帧是分别生成的画，整幅线条连桌腿都有漂移，轮播会抽搐；现在每个状态只用一帧，手、笔、头、眼在 2 像素内移动，每帧 50–70 ms，全部加眨眼（眼皮用各自脸颊肤色）；生成时检查桌腿区逐像素不变。用户要求：丝滑、小幅（写字、敲键盘、转头），思考幅度要小，递交报告仍只递一次。原图条保留，删掉 `motion/motion.json` 即回退。
- 2026-09-15：用户反馈只看得到眨眼（眨眼“挺丝滑，挺好的”），敲键盘太小看不清，写字要看到笔在动，放大镜要来回扫，递交报告时加整理文件的动作。改为“部件分层”：手、笔、放大镜、纸从内部种子按固定颜色范围漫延选到描边，抠成一层平移、旋转 2–5 像素，原位置用 inpaint 补齐；头眼仍用局部变形。递交报告先整理文件（后面两张纸伸出、磕两下对齐）再停住。播放器改为按需解码图条（最近 4 段留在内存）。
- 2026-09-15：应用之前没有图标，访达里是白纸一张。新增 `YukioPlayer/tools/icon/make_icon.py`，用分辨率最高的源图（`sources/read_web-corrected-source-2x2.png` 左上格，627×627）抠像后裁成头肩半身，配深蓝渐变与 macOS 的连续圆角方块（1024 里 824，超椭圆指数 5，与本机系统图标实测一致），生成 `Resources/AppIcon.icns`（16–1024 十个尺寸）；`build-app.sh` 一并打包并写 `CFBundleIconFile`。用户看过深蓝、浅色两版后选了浅色（脚本默认），`--style night` 仍可出深蓝版。

- 2026-09-16：用户自行生成了两个新状态的素材包（`yukio-notifications-CC.zip`：完成任务勾选卡、对用户提问问号卡，384×416 透明 PNG／WebP／GIF，各 3 帧）。包里那 3 帧是分别生成的画，帧间 17–22% 的像素在变、整个人横向漂移 2–3 像素、桌腿靴子一带每帧七千到九千像素在动，直接连播就是老的“呼吸抽搐”，因此**没有采用它的动画文件**，只取第 3 帧（`hold.png`，也就是包里 192 图集的第 3 格，定位已与现有坐姿一致：顶 22、底 197、中心 96）当底图，动作照 `tools/motion` 生成。2 倍原图留在 `sources/*-hold-2x.png`，以后升 Retina 用。
  新增状态 `question_for_user`（`AskUserQuestion`，原来显示空闲）和 `task_complete`。用户选择“先递报告再切勾选卡”：回答后先显示递交报告 `respondHoldMs`（3 秒，够播完整理文件那一下），再换勾选卡停到 `respondLingerMs`（8 秒）结束。动作：问号卡状态是指着卡的手点两下再抬眼看你；勾选卡状态是双手托卡轻轻抬起再放回。抬卡会露出卡底下的一条带，Telea inpaint 在那里是糊团，新增 `extend_rows()`（按行左右补齐，桌沿桌面横向均匀，比 inpaint 像）。
  这两个底图和其余坐姿不是同一批画的，桌腿差两三像素，但落在现有状态彼此的差距范围内。54 个测试通过；真实转录回放里两个状态都按预期出现。

- 2026-09-16：勾选卡改成“举着等你点”。原来是递交报告 3 秒 → 勾选卡 → 任务结束满 8 秒回空闲；现在牌子举起来就不自己放下，一直举到用户点雪绪一下（或用户在那条聊天里发下一条，牌子自动让位；中断、会话记录被删也放下）。点击时用桌面版 Claude 自己注册的深链 `claude://code/continue?session=local_…` 跳回那一轮任务所在的聊天，然后放下牌子。
  转录里的会话 ID（`~/.claude/projects/*/<会话>.jsonl` 的文件名）和桌面版的会话 ID 不是一个：对应关系在 `~/Library/Application Support/Claude/claude-code-sessions/<账号>/<组织>/local_<id>.json` 的 `cliSessionId` 字段（文件开头 60 字节内，只读；新增 `ClaudeSessionLinks.swift` 解析，按最近修改排序找，命中约 0 ms、全扫不中约 60 ms）。这是桌面版的内部记录、不是公开接口，可能随版本变化；对不上时（终端里跑的 Claude Code）只把 Claude 带到前面，不乱跳。本机实测：深链能把桌面版打开到指定会话（日志 `Warming up session local_…`），`--chat-link <会话ID>` 可查对应关系。
  `RouterConfig.respondLingerMs` 改名 `completeArmMs`，含义也变了：不再是停留时长，只决定“多久之内结束的任务才举牌”，免得播放器启动时回放到几小时前的旧记录又举一块过期的牌（重启会丢掉举着的牌子，这是取舍）。气泡文字“已完成”改成“已完成 · 点我打开对话”，让人知道可以点。菜单举牌时多一条“打开这条聊天并放下牌子”（鼠标不好对准时用）。61 个测试通过。
  未做（用户没要）：问号卡（等你拿主意）也点一下跳过去；点击其余状态不做事。（问号卡 2026-09-18 补上了，见下。）

- 2026-09-16：打包发布给别人下载。新增 `YukioPlayer/scripts/package-release.sh`：本机只有 Command Line Tools，`swift build --arch` 需要完整 Xcode，改成分别编 arm64 和 `--triple x86_64-apple-macosx13.0` 再 lipo 合成通用二进制，用 ditto 打成 `dist/Yukio-<版本>-macOS.zip`（保签名，约 6 MB）并附 sha256；`build-app.sh` 多接一个可选参数，传入已编好的可执行文件就不再现场编译。README 写了下载与首次打开的放行方法（临时签名、未公证：右键→打开，或到隐私与安全性点“仍要打开”）。发布在 GitHub Releases（v0.1.0）。

- 2026-09-18：菜单里的「大小」从 100% / 125% / 150% / 200% 四个固定档位改成一条滑条（`YukioPlayer/Sources/YukioPlayer/ScaleSlider.swift`，50%–200%，落点吸到整 5%）：拖着走，雪绪当场跟着变大变小，脚下中点不动，松手即存。滑条是菜单项自带的视图（`NSMenuItem.view`），菜单每次打开都重新建，滑块停在当前大小上；同一档内的鼠标移动不重复缩放窗口。读设置时也按同样的范围夹一次，设置坏掉不会出现 0 或大得离谱的雪绪。外观离屏自查过（浅色／深色、菜单宽 300 与 220 两种，文字与滑块都不挤），实际在菜单里拖动的手感仍须用户目测。
  Windows 版仍是四个固定档位：托盘菜单是 Win32 原生弹出菜单，塞不进滑条，要对齐得另开一个设置窗口。

- 2026-09-18：多个聊天同时跑时的跟随规则与挑选。以前的规则是“当前会话还在干活就不换”，于是另一条答完举的牌子会被正在干活的那条挤掉（旧 README 把这条写在已知限制里）。用户要求：完成提示和问题提示优先显示并自动切换聊天。现在 `ActivityRouter.updateFocus` 的顺序是：挑定的那条 → 等你处理的（勾选卡已举起或刚答完还在 `completeArmMs` 之内、`AskUserQuestion` 还没结束，多条都等时取最近的）→ 还在干活的那条 → 最近有动静的。举着的牌子因此会一直占着她，直到点掉、放下或那条聊天开始新一轮；菜单新增“先放下牌子，不打开聊天”作退路。
  菜单新增 **跟随的聊天** 子菜单（`SessionSummary` + `ActivityRouter.sessionSummaries`）：第一条“自动（完成和提问优先）”，下面列最近半小时、最多 10 条聊天，按“等你处理 → 在跑 → 安静时间”排，每条显示聊天名（会话标题，没有就用请求第一行）和此刻在干什么；点一条就挑定（`pinSession`），立刻切过去且别的聊天抢不走，挑定只在本次运行内有效（和举着的牌子一样，重启回自动）。列聊天时 `desired(for:arming:false)` 只看不动，不会顺手把别的聊天的牌子举起来。菜单第二行从“会话 xxxxxxxx… · 进行中 · 未完成工具 N”改成“正在跟：<聊天名> · <在干什么>（挑定的）”。
  新增命令行 `--chats [秒]` 列这份表（`→` 标出会跟哪条），`--watch` 多打印一行“跟随切到 [会话]”。70 个测试通过（新增 `ChatPickTests.swift` 9 个）；本机实测：4 条真实聊天的列表、标题与状态都对得上。
  Windows 版（`YukioWin/`）同步了同一套规则：`router.py` 的优先级、`session_summaries`／`pin_session`，托盘菜单的“跟随的聊天”子菜单（Win32 菜单只有打勾一种标记，自动模式下正跟着的那条用 `→ ` 前缀区分），`--chats` 命令行。89 个测试通过（新增 `tests/test_chat_pick.py` 7 个，`test_app_logic.py` 里多一个走托盘菜单挑聊天的）。Windows 版仍没有“举着牌子等你点”那一套（勾选卡照旧停 8 秒自己放下），所以那边的“完成优先”只在这 8 秒内。
  同日用户追加：“牌子被晾超过 15 分钟就先让位，那条聊天没有更新时再举回来”。于是 `RouterConfig.signYieldMs`（15 分钟）+ `SessionModel.signRaisedAt`：焦点顺序变成 挑定 → 等你处理且牌子没晾过头 → 还在干活的 → **晾过头的牌子**（只有没人在干活、且当前这条也没东西可显示时才轮到）。牌子本身不放下，`completedSession` 在让位期间是 nil（她没在显示牌子），列表里那条写“举着牌子等你（先让位了）”。问号卡不加这 15 分钟：它本来就受 `staleOpenToolMs`（30 分钟失联）约束。81 个测试通过。Windows 版不需要这条（那边勾选卡 8 秒自己放下）。
  同日用户让“参考现在 GPT 宠物的处理方式”。扒了 ChatGPT 桌面版（`/Applications/ChatGPT.app` 的 `app.asar`，只读、没有复制代码）：宠物是个全局浮层（⌥Space 显隐，命令名 `openAvatarOverlay`／“Show or hide pet”），**不挑聊天**——每条会话／云任务出一张通知卡挂在她旁边成一叠（可展开、身上带数字徽标），卡上是标题 + 当前动作副标题（“Ran command”“Searched …”）+ 状态标签（Running／Needs input／Ready／Blocked）；宠物自己的动作取**排第一张卡**的状态。排序 `waiting=0 → failed=1 → review=2 → running=3 → idle=4`，同档按最近；卡片过期时间 failed 1 小时、waiting 24 小时、review 7 天，running 不过期。单卡可开、可消、可回复（行内输入）、可停任务、可对该任务静音；另有 ⌘⌥A“下一条需要你的聊天”（先等输入、再未读）、⌘⌥U 侧栏 activity view、“Clear all unreads”。
  照着对齐了两处（没有改成一叠卡片，那是另一套 UI）：一是**档位排序**——`attentionRank`：等你回答 → 整轮出错停住 → 答完举牌，同档取最近，出错的聊天从此也会抢过“还在干活”的；二是**答完必举牌**——新增 `armPendingSigns`，任何刚答完的会话都在 `respondHoldMs`–`completeArmMs` 之间把牌子举起来存着，不再因为“那会儿她在跟别的聊天”而丢掉那一轮的完成提示（挑定期间同理）。菜单“跟随的聊天”标题顺带报数（“2 条等你”／“3 条在跑”），列表也按同一套档位排，出错的显示“出错停住了”。86 个测试通过。
  接着用户说“做通知卡叠”，于是把那叠卡也做了：`Sources/YukioCore/ActivityCards.swift`（`ActivityCard`／`cards(now:)`／`dismissCard`／`muteCards`，排序复用焦点那套档位，消卡按“这一轮”记、下一轮自己回来，消掉的那条这一轮也不再抢焦点）+ `Sources/YukioPlayer/CardStack.swift`（透明子窗口 + 纯排版的 `CardStackLayout`，左色带、聊天名、当前动作、状态标签、✕，最多 3 张再折进“还有 N 条”，右键静音）。点卡正文＝打开那条聊天并收起，点 ✕＝只收起；卡与卡之间的缝隙仍然穿透（`updateMousePassThrough` 里按命中区域开关 `ignoresMouseEvents`）。菜单多一条“旁边显示别的聊天”。**她正在显示的那条不进卡叠**——头顶气泡已经在讲它，不重复。新增 `--cards out.png` 离屏自查（收起／展开各一格，2 倍图）。92 个测试通过。
  同日用户看完说“卡叠要用原有的、头顶上的卡片为基础，而不是在旁边新建一个”，于是把身侧那一套撤了：气泡的外观抽成 `CardLook`（圆角 7、白 0.94 底、淡描边、上行 10 号淡色 + 下行 11.5 号粗字），`BubbleLayout` 与卡片共用同一套；卡宽也改成气泡的 180。卡不再摆在身侧，而是**压着气泡往上叠**（`CardStackLayout.origin(bubbleTop:)`，气泡一变高矮或隐去就跟着挪；气泡关掉时从头顶线起叠），最要紧的那张挨着气泡。菜单文案“旁边显示别的聊天”改成“头顶显示别的聊天”。`--cards` 快照也改成画“气泡 + 上面那摞”。真机用假会话验过：气泡在头顶，上面依次是答完了（绿）、在跑（蓝）两张和“还有 1 条”。
- 2026-09-19：用户指出仿 GPT 仿错了，并给出确切要求：**平时只显示一张卡，点头顶那张才展开，展开后选一条当“主题聊天”顶掉原来那条，且不能和举牌时点她冲突**。先回去把 ChatGPT 那只宠物的显示机制核实清楚（`app.asar` 的 `avatar-overlay-native-page-*.js`，只读）：`Fo(y[0]).mascotState` —— 宠物的动作取**第一条**通知；其余收起，靠 `isNotificationStackCollapsed`／`canExpandActivityStack`／`Expand activity stack, {count} items` 展开，宠物身上带 `notificationBadge`（`y.length`）；排序 `ld()` waiting 0 → failed 1 → review 2 → running 3；单卡 `onActivateNotification`（打开）／`onDismissNotification`／`Mute task`；过期 failed 1h、waiting 24h、review 7d、running 不过期。之前默认摊三张是我错了。
  改成：收起时只有气泡 + 一条“还有 N 条 · 点一下挑”；点气泡或那条提示展开（`BubbleView.onClick` → `toggleCards()`，气泡平时仍穿透，只有“有别的聊天可挑或正摊开”时才收点击）；点摊开后的某张 = `pinSession(那条)` 换主题聊天并收起；✕ 只收这一张；右键给“打开这条聊天／不再提醒”；挑定后多一条“自动：谁要紧跟谁”；12 秒无操作自动收起；`maxHeight` 按气泡上方剩余高度算，放不下就少摊几张，绝不压到气泡。点她本人仍是“跳转 + 放下牌子”，两条路径分开。气泡文案“已完成 · 点我打开对话”改成“已完成 · 点她跳过去”，免得以为点卡片会跳转。`--cards` 快照顺便打印点击分区自查（每张卡正文／✕、自动、收起、空白处各落在哪），空白处返回 nil＝照旧穿透。92 个测试通过。
- 2026-09-19：用户报“连正常状态下都打不开、什么都不显示”。原因不是这次的卡片改动：她的位置被记在已拔掉的外接屏上（originX=1470，主屏只有 1280 点宽），窗口整个在屏幕外；同时后台还留着一个 `build/Yukio.app` 的实例，所以再点 `~/Desktop/Yukio.app` 会被单实例逻辑挡掉、看着就是“打不开”。处理：清掉 originX/originY、关掉残留实例、重开。并加了防呆——监听 `NSApplication.didChangeScreenParametersNotification`，接／拔显示器或改分辨率时若她不在任何屏幕上就挪回默认角落（`screensChanged()`）。

- 2026-09-18：问号卡也能点了（上面 2026-09-16 那条列作“未做”的一项）。用户要求：举起问题牌子时点击也应该可以跳转到聊天。`ActivityRouter` 新增 `askingSession`（正立着问号卡的那条聊天，和 `completedSession` 对称，都按当前显示的牌子判断），`petClicked` 按牌子分两路：勾选卡点完打开聊天并放下牌子（那一轮已经结束），问号卡点完只打开聊天、卡片不收——问题还等着你答，等 `AskUserQuestion` 结束它自己收。菜单在立着问号卡时多一条“打开这条聊天去回答”。气泡文字从“等你回答”改成“等你回答 · 点我打开对话”，和勾选卡一样让人知道能点；聊天列表用的 `stateText` 没动，那份列表里不该带这句。
  70 个测试通过；离屏画了气泡快照，这行字没被截断（和“已完成 · 点我打开对话”一样宽）；回放本机一条带 `AskUserQuestion` 的真实转录，问号卡和新气泡文字都按预期出现。实际点上去跳不跳仍须用户目测。

- 2026-09-18：拖动时的动作从左右跑动改成「被一只看不见的大手拎着」，并按物理规则晃。
  素材是这次用 GPT（ChatGPT 的图像生成，chatgpt.com）新生成的：先给它现有的站立图与高清坐姿图做身份参考，
  第一版姿势仍像站着，补一句「像拎小猫一样捏着后领」才对；最终图透明背景 1024×1536，留在 `sources/held-source.png`，
  提示词记在 `references/generation-prompts.json` 的 `held`。`YukioPlayer/tools/hang/make_held.py` 把它裁成 192×240 的
  `assets/base/held.png`：**按头宽而不是整体高度缩放**（头一样大才不会觉得人物忽大忽小），领口的尖对到中线、
  头发顶端对到与站立图相同的行，所以抓起来的一瞬间头不动、领口从头顶上方被捏起来。
  晃动在 `Sources/YukioCore/HangSwing.swift`：绕抓手点的单摆 + 阻尼 + 空气阻力，手往右甩她落在后面，
  手停住她越过竖直荡回来，匀速走时略微后仰，松手加大阻尼、0.3–0.8 秒停稳再换回当前活动。
  手的加速度打了折（`handCoupling`）：严格按「一点当一毫米」算，随手一拖就顶到最大角，每次都一个样。
  用户看过第一版（0.35、最大 34°）说「晃动幅度太大」，当天改成 0.16、空气阻力 0.9、最大 20°。
  用户接着要求「删掉头顶的尖锐三角、增强重量感」，于是：
  ① GPT 画的那个「被捏起的领口」深蓝三角整块抠掉（`drop_collar_peak()`：从尖内部按深蓝色漫延，
  连抗锯齿边缘一起清，只清头发轮廓以上的部分）。抓手点因此不再对着图上的东西，改成头发顶端上方 6 点的空点
  （横向取人物重心，吊起来才是正的）——那只大手本来就是看不见的。
  ② 重量感：摆动周期 0.62→0.9 秒（沉的东西摆得慢，摆幅照旧约 10°，所以 handCoupling 0.16→0.11、
  空气阻力 0.9→0.43），手速低通 0.045→0.07 秒（起步慢半拍），阻尼 0.16/0.5→0.13/0.4；
  新增竖直方向的「会伸缩的布」：手猛地往上提，她先坠下去（上限为摆长的 16%，约 15 点）再弹回来，
  纯左右拖不坠。渲染时整张图按坠的距离往下挪，旋转中心仍是抓手那一点，窗口底下多留 15 点。
  拎起来时窗口临时放大到 340×309 容下摆动与下坠，放下再还原；贴边、记位置、气泡都按平时那块 192×208 算。
  ③ 用户随后指出「移动过程中摆动方向完全违背物理规律」——查下来物理是对的（往右拖角度为负＝脚在后面），
  是**画反了**：旋转写成了 `-angle`。图层坐标 y 向上、脚在抓手点下方，脚往右是逆时针，要用正角
  （钟面上指向 6 点的指针顺时针是往左走的，这里当初想反了）。命中测试的反向旋转、两个快照工具一并改正。
  `--hang` 现在每次自检：按 ±20° 画出来量脚的横向位置，再单独算一次 PetView 用的矩阵，
  两边都得是「正角＝脚偏右」，否则非零码退出。改完从渲染出的 GIF 上实测：往右拖脚落在左后方，
  停下后荡过竖直线到前面再收住，往左甩相反。
  没有录屏权限，改用两个命令行检查：`--hang`（五个倾角并排，带窗口框与抓手点）、`--hang-gif`（真实逻辑演一遍存成 GIF）。
  摆动测试 13 个（静止不晃、落在后面、越过竖直、越晃越小、匀速后仰、甩不翻、放大摆幅更小、松手后很快停稳、
  再抓住接着晃、休眠很久回来不炸，以及提起来会坠、纯横拖不坠、坠有上限），连同资源加载共 16 个本次相关的测试通过。
  未做：被拎着时只有一帧、不眨眼（`tools/motion` 的流水线按 192×208 写死，要眨眼得先放开尺寸）；
  Windows 版 `YukioWin/` 仍是左右跑动，没有跟着改。

- 2026-09-18：用户反馈“待机时腿的比例跟拎起来时不一样”，要求把站姿改成和移动（被拎起来）时一个比例。量过三套：待机头宽 75、坐姿 84–87、拎起来 76——**待机和拎起来的头一样大，差的是身子和腿**（待机裙摆到鞋底 50 px，拎起来 77 px）。没有重画，新增 `YukioPlayer/tools/proportion/restretch_idle.py`：只把裙摆下、靴口上那截白袜纵向拉长（20 → 34 px，纯白，拉了看不出接缝），头／上身／裙／吊带／袜口／靴子逐像素不动，整体上移让头顶顶到 y2，**鞋底仍在 y195**（站的位置不变）。待机和沮丧两套都改了（都是站姿，只改一套会互相打架），`base/` 与 `motion/` 同名文件一起，原图备份在 `sources/pre-restretch/`。腿占身高从 22% 提到 33%（拎起来是 38%）；要完全一致得让站姿也用 240 高的画面并改窗口高度与地面线，没做。
  连带改动：站姿头顶线从 y16 变成 y2，和坐姿（y22）差一截，原来全局取最小值的做法会把所有气泡顶高 14 px，所以 `SpriteLibrary` 改成按段记头顶线（`headTopInset(for:)`），气泡与身侧卡叠贴各自那一段，换动作时重新贴一次。92 个测试通过；`--check`、`--snapshot`、`--bubble` 离屏都看过。

- 2026-09-18：把上面这一整批（9-16 起）同步到 Windows 版 `YukioWin/`。此前那边只跟到「挑一条聊天跟」，之后的都没过去。逐条对齐：
  **举着的牌子**：`RouterConfig.respond_linger_ms` → `complete_arm_ms`（含义同 macOS：只决定多久之内结束的任务才举牌），`_Session.sign` 三态（没举／举着／点过了）+ `sign_raised_at` + `sign_yield_ms`（15 分钟让位），`completed_session`／`asking_session`／`dismiss_completion`。勾选卡不再停 8 秒自己放下。
  **档位与不丢牌**：`_attention_rank`（等你回答 → 出错停住 → 答完举牌）、`_failed_pending`、`_arm_pending_signs`，焦点与聊天列表、卡叠共用这一套。
  **头顶那摞卡**：`yukio/cards.py`（`ActivityCard`／`CardStatus`）+ 路由上的 `cards()`／`dismiss_card`／`mute_cards` + `yukio/cardstack.py`（纯排版的 `CardStackLayout`，和气泡共用 `bubble.py` 那套圆角、描边、字号）。第三个分层窗口 `YukioCards` 收点击，卡与卡之间的缝隙靠透明像素穿透；点正文＝去那条聊天并收起，点 ✕＝只收起，右键＝静音那条聊天。菜单多一条「头顶显示别的聊天」。
  **点一下跳回聊天**：`yukio/chatlinks.py` 照着 `ClaudeSessionLinks.swift` 写，路径换成 `%APPDATA%\Claude\claude-code-sessions`，深链仍是 `claude://code/continue?session=local_…`，`win32.open_url()` 走 ShellExecute（只放行 claude/http/https 三种协议）。**在这台 Mac 上拿真实记录验过解析**（`--chat-link` 查得出 `local_…`，和 Swift 版同一个结果）；Windows 上的路径与协议注册没有实机验过，对不上时只放下牌子。Deep Code 的会话跑在终端里，本来就没有这种链接。
  **被拎起来**：`yukio/hang.py` 是 `HangSwing.swift` 与 main.swift 里 `HangGeometry` 的移植，参数逐个照抄（0.9 秒一摆、阻尼 0.13/0.4、空气阻力 0.43、handCoupling 0.11、最大 20°、坠最多摆长的 16%）。窗口几何算出来和 macOS 一样是 340×309，摆长 95 px。渲染用 Pillow 的 `rotate(+angle)`：y 向下的位图里「视觉逆时针」正好等于脚偏右，和 macOS 那边 CoreGraphics 的正角同向；`--hang` 每次画完自查一遍方向（画出来量脚的位置 + 按矩阵算各一次，反了就非零退出），免得重蹈 macOS 那次把旋转写成 `-angle` 的覆辙。左右跑动的图条不再加载（`_USED_BASE` 换成 `held`），文件还在。
  **比例与头顶线**：素材本来就跟 macOS 版共用一份，拉长腿的那次自动跟着变；`SpriteLibrary` 同样改成按段记头顶线（`head_top_inset_for`），`head_top_inset` 只作兜底，`held` 不参与取最小值。顺带把仓库根 `assets/base/` 里的 `idle.webp`／`failed.webp` 也换成拉长后的那两张（此前只改了 `YukioPlayer/Resources/Assets`，两处不一致）。
  **大小**：Win32 的托盘菜单是系统原生弹出菜单，塞不进 macOS 那条 50%–200% 的滑条。跟用户确认后改成七个整档（50/75/100/125/150/175/200%）加「放大一点／缩小一点」各 ±5%，范围与步进和滑条一致，读设置时同样夹一次（设置坏掉不会出现 0 或大得离谱的雪绪）。要完全一致得另开一个设置窗口，没做。
  **命令行**：新增 `--cards`（气泡 + 上面那摞，收起与展开各一格）、`--hang`（五个倾角并排 + 方向自查）、`--chat-link <会话ID>`；`--check` 改报各段头顶线与摆长。
  129 个测试通过（原 89 → 新增卡叠 6、摆动与几何 16、深链 4、聊天挑选 5、播放器逻辑 5，另有若干条按新语义改写）。离屏画过 `--cards`、`--hang`、`--bubble`、`--snapshot`；`--replay` 走完样例记录，结尾停在举着的勾选卡上；`--chats` 对着本机真实的 Claude 会话列得出名字与状态。没测到的仍是 Windows 上的系统调用本身（分层窗口、托盘、弹菜单、深链注册）。
  顺手修掉一个小问题：`tools/proportion/restretch_idle.py` 里 `ROOT` 少退了一层，备份写到了 `YukioPlayer/Sources/pre-restretch/`（macOS 文件名不分大小写，混进了 `Sources/`）。改成仓库根，已有的两份备份挪回 `sources/pre-restretch/`。

## 已完成与未完成

已完成：生成、提取与保存七套活动素材；静态电脑桌；基础跑动与待机图条；手动 HTML 预览；单宠物原生图集快照。

未完成：独立桌面窗口、可安装应用、真实活动事件来源、分类器、线程选择、事件路由、防抖、自动动画切换、断连回退、macOS 实测。

预览提供 `window.setYukioActivity(id)`，只改变本页展示；没有连接 Codex、没有操作系统窗口，也不是后台监听程序。未知 ID 在该演示中回到电脑桌。

## 当前本机原生宠物

- ID：`yukio-codex-maid`；显示名：雪绪。
- 仅这一只雪绪保留在本机宠物目录。
- 当前图集是 `spritesheet-thinking-v5.webp`，与 `native-current/pet.json` 配套。
- 用户最后要求“把旧版思考改成新版”，因此只替换了原生 `running / Thinking` 槽位（第 7 行）为新版托腮。
- 原生待机（第 0 行）、左右跑动（第 1、2 行）、桌前阅读／完成画面（第 8 行）和其余图格未改动。
- **原生 Thinking 和其他工作共用同一槽位**，所以目前原生其他工作也暂时显示托腮。这是临时兼容方案，不是独立播放器目标；新播放器的未知工作仍应显示电脑桌。

## 原生限制：来自当时本机实现的观察

这些是检查当时安装版本后的结论，不是对未来版本的永久保证：

- v2 宠物：1536×2288，8 列×11 行，每格 192×208，PNG／WebP 图集。
- 原生只有较粗的 `idle/running/waiting/review/failed`，加跑动与交互槽位；没有每种工具活动的动画绑定字段。
- 观察到的 `pet.json` 字段只支持 ID、名称、描述、spriteVersionNumber 与 spritesheetPath。没有 FPS／自定义活动路由配置。
- 当前待机时序为 1680、660、660、840、840、1920 毫秒，共 6.6 秒。其他状态有自己的短时序，通常播三遍再回待机。
- 原生会缓存图集；反复改磁盘文件不等于能实时驱动已经显示的宠物。
- 原生 `review` 是结果准备好待查看，不是“正在做测试”；执行测试仍属 `running`。

本包只保留这些结论，没有复制应用内部打包代码。真实独立播放器接入方式仍须验证。

## 素材规格与质量边界

- 七套活动 WebP 都是静态横向图条，每套 4 格，总尺寸 768×208。它们不是自行播放的 animated WebP。
- 电脑桌 PNG 为 192×208 单帧；`question_for_user.webp`、`task_complete.webp` 同样是 192×208 单帧底图（2 倍原图在 `sources/`）。
- 新状态需要的素材：动作小的一张主图就够（眨眼、头眼小幅动都是生成的）；要让手或道具动得明显，再加一张把那只手／道具擦掉、其余像素不动的干净底图；坐姿状态必须复用同一套桌椅和机位（状态切换是硬切，没有淡入）。
- `activities.json` 定义帧序列、停留时长、循环／最后一帧保持；这些是现有预览时序，可根据实际流畅度调整。
- `assets/base/base-animations.json` 包含基础图条及原生时序，仅供参考。独立播放器不必沿用原生“三遍后回待机”。
- 16 个视线方向从朝上 0° 开始，顺时针排列；正面中立姿势另有 `neutral.png`。
- 源图为生成素材，四帧之间仍可能有小幅线条与几何漂移；尺寸和文件校验通过不等于已通过真实桌面动画体验验收。
- 不要把“60 FPS 渲染循环”声称为已有“60 个不同动画帧”。要进一步丝滑，需要实际检查动作幅度、帧序列、局部素材或重新制图。

## 本包的取舍

保留当前可用素材、修正源图、原生单宠物快照、可独立打开的预览和接续说明。未包含被撤回的七宠物安装包、错误的平板图、最初变形跳跃、旧聊天日志、账号资料或 Codex 应用程序。

没有可用的桌面工程可以直接接着编译；下一阶段需要创建工程。既有本机脚本含硬编码路径，未作为 Cloud 构建依赖打包。最终透明素材已完整包含，源图也可继续提取或修整。

- 2026-09-20：界面语言改成默认英文、菜单里可切中文。用户的话是「把所有语言改成英文，保留一个可切换的中文版方案；软件运行时默认英文，右键设置里切换中文版」。
  两个播放器都加了 `tr(en, zh)`（macOS `Sources/YukioCore/L10n.swift`，Windows `yukio/l10n.py`）：菜单、头顶气泡、那摞卡、聊天列表、工具说明、演示脚本、托盘提示全部英文在前、中文在后。
  语言存在设置里（macOS UserDefaults `language`，Windows settings.json `language`），默认英文，菜单「Language」里选「中文」就切；命令行渲染按环境变量 `YUKIO_LANG`。
  启动时先定语言再回放会话记录，回放出来的说明文字才是对的语言；已经写进事件里的说明不回溯翻译，下一条事件起换。
  英文比中文占地方：180 点宽的气泡放不下 "Done · click her to jump back"，改成 "Done · click to open"、问号卡 "Your turn · click to open"，AskUserQuestion 的说明是 "Needs your answer"（Deep Code 等批准是 "Needs your approval"）。
  菜单第一行与托盘提示里的状态名不再取 activities.json 里的中文标签，改用 `L10n.stateName`／`state_name`（英文 Idle、Working…，中文 空闲、敲键盘…）。
  测试仍按中文文案断言：Swift 各 @Suite 的 init 里、Python 各测试模块的 setUpModule 里把语言切成中文（Windows 的应用测试给临时设置写 `"language": "zh"`，因为 PetApp 启动会按设置重设语言），另加 L10nTests／test_l10n 验英文默认。macOS 101 个、Windows 141 个测试通过。
  没动的：命令行诊断输出（--watch、--check、--hang 自检等）与代码注释仍是中文；Windows 版 `--bubble`／`--cards` 的样例文字也仍是中文。
  文档：根 README、两个播放器的 README 都改成英文默认，中文版为同名 `README.zh-CN.md`，顶部互链；CI 工作流改名 "Build Windows Yukio"；README 配图由 `docs/readme/make_images.py` 生成，英文界面的渲染是默认图，中文界面的带 `-zh` 后缀。

- 2026-09-23：有问题时可以直接在雪绪这边回答（用户的话是「添加回复问题功能。有问题时可以直接在雪绪这边回答。举牌时问题抄录下来在适宜的地方以适宜的大小显示」，并选了「模拟键盘直接打进去」「macOS + Windows 都改」）。两版同时改。
  **抄题**：新增 `PetQuestion`（macOS `Sources/YukioCore/PetQuestion.swift`，Windows `yukio/events.py`）：小标题、问题正文、选项（标题 + 说明）、是否多选。认三种写法——Claude 的 `questions:[{question, header, options:[{label, description}], multiSelect}]`、Codex 的 `questions:[{title, options:["…"]}]`、以及顶层直接 `question`／`prompt` 的简写。只有问话类工具才抄（`AskUserQuestion`／`request_user_input` 那几个名字），别的工具参数里叫 question 的字段与这件事无关。`PetEvent` 多一个 `question` 字段，四个解析器（Claude 转录、Claude hooks、Codex、Deep Code）在 `activity_start` 上带出来。顺带：`AskUserQuestion` 的气泡说明从固定的「等你回答」改成那道题本身（有小标题用小标题）。
  **显示**：`ActivityRouter.askingQuestion`／`asking_question` 给出「此刻在问什么」（会话 ID、那次调用的 ID、题目）。新增一张卡：macOS `QuestionCard.swift` + `QuestionPanel.swift`，Windows `yukio/question.py` + app 里第四个分层窗口。宽 280 点（气泡 180 点只够放一行状态），问题折到最多六行、超了截断，选项最多六个一行一个，立在她左边或右边看哪边放得下。举着问号卡就自己立起来，答完自己收。
  **回答**：点选项＝那个选项的文字就是答案（macOS 还可以按数字键 1–9）；多选题点过的打勾、按 ⏎ 一起送（用「、」连）；下面一个真正的文本框可以自己写一句（macOS `NSTextField`，Windows 单独一扇窗口里的 EDIT 控件，都是为了让输入法照常用）；✕ 只收这一道题的卡，换一道题会重新立。设置里多一个开关「在这儿回答问题」（`showQuestionCard`），关掉就还是老样子。
  **送出去**：macOS `AnswerSender.swift` 走「复制 → 深链把那家带到最前面 → 确认最前面的确实是它 → ⌘V → 回车 → 把粘贴板放回去」，需要系统的辅助功能权限（第一次弹系统面板）；Windows `yukio/answer.py` 走「复制 → 深链 → 等 `Claude.exe` 到前台 → `SendInput` 的 Unicode 模式逐字打 → 回车」，不需要额外授权。**两版都遵守同一条：最前面的应用不是预期的那个就一个键都不按**，只留粘贴板并在卡上说明；跳不过去的聊天（Deep Code 在终端里）只复制；演示里不送。
  **自查**：新增 `--question out.png`（两版都有）：离屏画出单选、多选、没有选项、已答过四格，并打印点击分区。Windows 的卡片里 ✓／⏎／› 换成 √／→／>（中文字体里前两个未必有，画出来是豆腐块），半透明填充先和纸色混好再画（Pillow 直接写像素、不做合成，不然会在卡上打洞）。
  **测试**：macOS 127 个（新增 7；并入 v0.2.0 的眨眼等测试后合计 135）、Windows 181 个（新增 26）。Windows 那 26 个里，5 个验送出的状态机（假的按键后端）：到了前台才打字、等不到就只复制、中途切走就收手、没有窗口可跳只复制、空答案不动作；8 个在假窗口层上把整张卡走了一遍：卡立在她身边、关掉开关只剩问号卡、点选项后打进聊天、前台不对就一个键不按、点输入框把真输入框盖到画出来的框上、✕ 只收卡、「打开聊天」只开不送。
  **没验到的**：两边真正按键的那一步都没有在活的聊天窗口上跑过——macOS 缺屏幕录制／辅助功能授权的实测，Windows 本机没有。Claude 桌面版在 `AskUserQuestion` 弹着选项时，输入框收不收这一行字、收了算不算回答，也没有实测过；收不下时表现为「答案打进了输入框但没被当成答案」，退路是卡上的「打开聊天」。

- 2026-09-25：发 v0.2.1。上面「在雪绪这边回答问题」原本是在 v0.2.0 之前的 768508f 上改的、没提交；这次把它合到 v0.2.0（3f9be04）之上，只有 README 测试行冲突。合完 macOS 135 个、Windows 181 个测试通过；版本号改 0.2.1（`build-app.sh`、`yukio/__init__.py`、README 下载表）。
