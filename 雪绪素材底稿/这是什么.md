# 雪绪的素材底稿

从仓库 `~/Desktop/yukio-cloud-handoff/` 里搬出来的原始素材包，程序用不到，
留着当底稿：要重调动作时长、改尺寸、或者再生成一版时，从这儿开工比从头来快。

## yukio-notifications-CC.zip（2026-09-16，3.1 MB）

举牌那两个动作的毛坯料：

- **task-complete** —— 举起绿色勾选卡（任务答完）
- **question-for-user** —— 立起问号卡（要你拿主意）

每个动作都有：会动的 webp / gif、拆开的三张 png 单帧（384×416）、
一行三格的拼图（每格 192×208）、定格用的 hold.png。
另有 `preview.html`（双击打开，点按钮重播）和 `generation-prompts.json`（当初的生成提示词）。

成品已经在仓库里了：`YukioPlayer/Resources/Assets/motion/task_complete.webp`
和 `question_for_user.webp`（压过，约 12 万字节；包里的原件 25 万）。
